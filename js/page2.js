var selectedOption = document.querySelectorAll('#algo');
var selectedVal = selectedOption.id;
var roundrobin = document.getElementById('roundrobin');
if(selectedVal == "roundrobin"){
    roundrobin.style.display = "block";
}